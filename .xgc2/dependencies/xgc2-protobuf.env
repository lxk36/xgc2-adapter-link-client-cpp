# RuntimeLink protocol line consumed by this SDK. Debian revision selection belongs
# to the active release train, rather than to this source checkout.
XGC2_PROTOBUF_PROTOCOL_VERSION="0.5.0"

# Standalone CI builds this immutable source revision when no release overlay is
# present. Release workflows instead install the exact candidate from their
# signed overlay.
XGC2_PROTOBUF_STANDALONE_SOURCE_REF="cd0b18754f6fb4d66fcd99b5d95032f693c391b4"
